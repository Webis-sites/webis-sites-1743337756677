import { generateComponent } from './service';
import type { GeneratedComponent } from './types';

export {
  // Main function to generate a component
  generateComponent
};

// Export types
export type { GeneratedComponent }; 